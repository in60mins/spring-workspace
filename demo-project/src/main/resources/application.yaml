spring:
  application:
    name: demo-project
  datasource:
    url: jdbc:mysql://localhost:3306/spring_boot
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update